## Future Improvements

- Move client-side filtering to server-side when dataset grows.
- Introduce pagination for large transaction lists.
- Persist selected card in URL (query param) for deep linking.
- Add sorting by date and amount.
- Add accessibility improvements (ARIA roles).
- Extract design tokens into a centralized configuration for better scalability.
- Add end-to-end tests (e.g., Playwright) for real browser-level validation.

